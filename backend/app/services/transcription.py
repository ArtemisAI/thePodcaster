"""Whisper / Faster-Whisper wrapper functions."""

# Main function prototype:
#
# def transcribe(file_path: Path, model_size: str = "medium", language: str | None = None) -> dict:
#     """Return JSON payload with segments, text and word-level timestamps."""

# TODO: implement with faster-whisper; include GPU/CPU auto-selection
