"""Wrapper for interacting with n8n workflows."""

# Provide functions such as:
#
# async def trigger_youtube_upload(video_path: Path, title: str, description: str) -> str:
#     """Return workflow execution ID."""
#
# async def trigger_rss_update(audio_url: str, metadata: dict) -> str:
#     """Return workflow execution ID."""

# TODO: send HTTP requests to the configured n8n webhook endpoints
